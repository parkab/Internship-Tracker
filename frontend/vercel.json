{
    "version": 2,
    "builds": [
      {
        "src": "src/main.ts",
        "use": "@vercel/static-build",
        "config": {
          "distDir": "dist"
        }
      }
    ],
    "routes": [
      {
        "src": "/(.*)",
        "dest": "/dist/index.html"
      }
    ]
  }